<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_tag(base_test_erb_util_perform_path(encode_method(params[:method]), params[:option])) do %>
  <%= render "simple_input" %>
  <div class="actions">
    <%= submit_tag "Submit" %>
  </div>
<% end %>