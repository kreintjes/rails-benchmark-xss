<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_tag(base_test_tag_helper_perform_path(encode_method(params[:method]), params[:option])) do %>
  <%= render @partial %>
  <div class="actions">
    <%= submit_tag "Submit" %>
  </div>
<% end %>